# Groq_Chat_Bot

This ChatBot is using Groq 
steps to run this app:-
  1)First create virtual environment 
  2)install requirements.txt file 
  3)create .env file and add api_key variables in that file
  4)finally, run the app
